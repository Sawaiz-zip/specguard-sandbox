# Expense Entry

## Summary
Add, edit, and delete expenses from the command line, stored in a local plain-text ledger.

## In Scope
- `ledgerly add` with amount, category, date, and note
- `ledgerly edit` / `ledgerly rm` for existing entries
- Validation of amounts and dates

## Out of Scope
- Importing directly from bank or credit-card APIs
